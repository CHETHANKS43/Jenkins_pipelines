
    pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/CHETHANKS43/Jenkins_pipelines.git'
                echo 'this is the first build'
            }
        }
    
    }
}
